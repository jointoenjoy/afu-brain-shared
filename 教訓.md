# 教訓（錯誤 → 規則）

> 阿福開工前先讀這份。每次出錯，就把「發生什麼 → 下次怎麼避免」寫成一條規則，避免重犯。

## 格式
```
### YYYY-MM-DD 一句話標題
- 發生什麼：
- 為什麼錯：
- 下次規則：
```

## 規則清單
### 2026-06-08 風險不確定時先問人
- 發生什麼：（範例）阿福誕生，先立一條基本規則。
- 為什麼錯：自動化最大的風險是「該問人的時候沒問」。
- 下次規則：任何牽涉資料庫、金流、個資、刪除性操作，先停下來請對應的人確認，不確定就往高一級風險靠。

### 2026-06-11 一個對話開太久 → 打到用量上限
- 發生什麼：下午一個 session 連開 495 回合都沒重開，cache_read（系統每回合重讀整包脈絡的量）累計 1.3 億 tokens、平均每回合重讀 26 萬字，中途還把 Figma 截圖／Canva／Google Drive 大檔塞進主線，雪球越滾越大，最後打到用量上限。
- 為什麼錯：對話越長，每送一句都要把前面累積的脈絡整包重讀一次，成本是「越後面越貴」、近乎平方成長。一直在同一個對話裡做完一整個下午，等於每句話都付全額重讀費。
- 下次規則：
  1. **分段開新對話**：一個任務告一段落、或脈絡偏大時就 `/clear` 換新對話，別把一整天疊在同一個 session。
  2. **重活派分身**：要「讀很多／搜很多／做很多、最後只要結論」的活，優先派 `.claude/agents/` 三隻分身（只回結論、不佔主線脈絡），不要自己在主線硬讀。
  3. **大檔別進主線**：Figma 截圖、Canva、Google Drive 等吃脈絡大戶，盡量在分身裡處理或只取必要摘要，不要整包塞進主對話。
  4. **已加自動防線**：`~/.claude/hooks/context-guard.sh`（用量守門員，掛 UserPromptSubmit）會在脈絡約 15 萬／28 萬字時自動提醒換對話。看到它出聲就照做。

### 2026-06-20 新技能做好卻沒列進名冊 → aiflow 儀表板看不到
- 發生什麼：兩週內新做了 6 個技能（前端品味、Ai-design-flow、教練、資安-外來專案掃描、民調縮影、趨勢保鮮檢查），資料夾都在，但 aiflow 儀表板上看不到。
- 為什麼錯：儀表板（generate-status／build-status）是**只讀 `tools/dashboard/agents-roster.json` 名冊**來列技能，不是掃 `skills/` 資料夾。技能做好了若沒同步進名冊，等於對儀表板隱形。
- 下次規則：
  1. **每次新增技能，立刻把名字補進 `tools/dashboard/agents-roster.json` 對應類別**（名字要跟資料夾名一字不差），這步跟寫 SKILL.md 綁在一起做。
  2. **已加自動防線**：`.claude/hooks/開工提醒.sh` 每次開工會比對 `skills/` 資料夾 vs 名冊，有漏列就出聲 ⚠️。看到就補。
  3. 名冊只是「源頭」；要真正反映到線上儀表板，仍需在 Mac 跑 `build-status.mjs` ＋ wrangler 部署。
